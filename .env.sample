# Flask
FLASK_DEBUG=1
SECRET_KEY=dev-change-me

# App database (MVP / local):
DATABASE_URL=sqlite:///campaigns.db

# --- Read-only Network NPI Source (Postgres recommended) ---
# Point this to your warehouse/read replica user that has SELECT ONLY.
# Example:
# NETWORK_DATABASE_URL=postgresql+psycopg://readonly_user:pass@host:5432/warehouse
NETWORK_DATABASE_URL=

# Provide a SQL query that returns one column named 'npi' (text/varchar)
# This is the only requirement for matching and counting.
# Example:
# NETWORK_NPI_SQL=SELECT DISTINCT CAST(npi AS TEXT) AS npi FROM public.network_npis
NETWORK_NPI_SQL=
